-- Testing spaces
constant value  << 2137
var      value  <- "XD"
VERY BIG NUMBER <- 1 234 456 789

-- Testing string literal
string <- "Test \"test\" TEST"

-- Array literal
array <- [2, 1, 37, "papież", "hehe"]

-- Array access
hehe <- array[4]
array[1] <- 13

-- Object literal
what can I do <- {
        kill yourself << false,
        go to bed <- "maybe",
        wake up at <- 8.5,
        backpack <- [
                "laptop", "charger", "mouse",
                "notebook", "pen"
        ],
        attitude <- {
                positive <- false,
                wanna die <- true
        },

        and after that() -> eat breakfast()
}

-- Object field access
what can I do :: go to bed <- "definitely"
don't do that <- what can I do :: kill yourself

-- Nested field access
what can I do :: attitude :: positive <- true
what can I do :: attitude :: wanna die <- false